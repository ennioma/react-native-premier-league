'use strict';

var React = require('react-native');

var {
  StyleSheet,
} = React;

module.exports = StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: 'row',
    backgroundColor: '#FFFFFF'
  },
  teamTitle: {
    marginTop: 64,
    fontSize: 15,
    textAlign: 'center',
    color: '#4C2D73',
    height: 35,
  },
});
